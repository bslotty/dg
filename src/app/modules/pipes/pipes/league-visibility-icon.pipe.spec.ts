import { LeagueVisibilityIconPipe } from './league-visibility-icon.pipe';

describe('LeagueVisibilityIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LeagueVisibilityIconPipe();
    expect(pipe).toBeTruthy();
  });
});
