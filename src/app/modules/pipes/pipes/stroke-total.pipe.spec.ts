import { StrokeTotalPipe } from './stroke-total.pipe';

describe('StrokeTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new StrokeTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
